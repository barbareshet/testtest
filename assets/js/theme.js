/**
 * Theme scripts
 *
 * @package MJK
 */

(function () {
    // modal._init();
})();

window.onload = function () {
	window.browserHistory = window.History.createBrowserHistory();
    blog._init();
    googleMaps._init();
    slickCarousel._init();
    sectionTransition._init();
    imageModal._init();
    tabs._init();
    video._init();
    tabs._init();
    lazyLoad._init();
    formModal._init();
    header._init();
};
